# THE AIR：Lom，帶公主逃！

手機與電腦皆可玩的粉絲向閃避遊戲。

## 上傳 GitHub Pages

1. 解壓縮下載的 ZIP。
2. 將 `index.html`、`style.css`、`game.js`、`README.md` 與整個 `assets` 資料夾全部上傳到儲存庫根目錄。
3. GitHub「設定 → 頁 → 從分支部署」，選擇 `main` 與根目錄 `/`。
4. 等待約 1～3 分鐘後重新整理遊戲網址。

請勿只上傳 ZIP，也不要更改 `assets` 資料夾或圖片檔名，否則圖片會無法顯示。
